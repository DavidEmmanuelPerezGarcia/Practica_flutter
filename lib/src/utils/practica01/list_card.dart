import 'package:flutter/material.dart';

class ListCards{
  String title;
  Widget contain;
  String text;

  ListCards({
    required this.title,
    required this.contain,
    required this.text
  });
}